<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luu.druid.druid_demo.mapper.TestMapper">


    <select id="test" resultType="java.lang.Integer">
        select 1
    </select>

    <select id="mallNoAnno" resultType="java.lang.String">
        select id from oms_order where id = '12'
    </select>
    <select id="mallExitAnno" resultType="java.lang.String">
        select id from oms_order where id = '13'
    </select>
    <select id="userNoAnno" resultType="java.lang.String">
        select username from app_user where username = 'admin'
    </select>
    <select id="userExitAnno" resultType="java.lang.String">
        select username from app_user where username = 'admin'
    </select>
</mapper>